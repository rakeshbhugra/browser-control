{
    "type": "function",
    "name": "read_page",
    "description": "Reads and returns the content of the specified URL.",
    "parameters": {
        "type": "object",
        "properties": {
            "url": {
                "type": "string",
                "description": "The URL of the page to read."
            }
        },
        "required": ["url"],
        "additionalProperties": false
    }
} 