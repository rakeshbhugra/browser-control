{
    "type": "function",
    "name": "input_to_element",
    "description": "Given the element info, inputs the provided text to the element using Playwright's page.locator(locator).fill() method",
    "parameters": {
        "type": "object",
        "properties": {
            "text": {
                "type": "string",
                "description": "The text to input."
            },
            "locator": {
                "type": "string",
                "description": "The locator for the element to input text into."
            }
        },
        "required": ["text", "locator"],
        "additionalProperties": false
    }
} 