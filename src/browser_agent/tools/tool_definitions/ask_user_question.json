{
    "type": "function",
    "name": "ask_user_question",
    "description": "Asks the user a question and gets their response.",
    "parameters": {
        "type": "object",
        "properties": {
            "question": {
                "type": "string",
                "description": "The question to ask the user."
            }
        },
        "required": ["question"],
        "additionalProperties": false
    }
} 